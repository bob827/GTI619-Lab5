@using GTI619_Lab5.Utils
@model string
@{
    ViewBag.Title = "Index";
}

<h2>Welcome, @SessionManager.GetLoggedInUsername()</h2>

@if (!string.IsNullOrEmpty(ViewBag.Message))
{
    <div class="alert alert-success" role="alert">@ViewBag.Message</div>
}