@model GTI619_Lab5.Models.Admin.IndexModel

@{
    ViewBag.Title = "Index";
}

<h2>Users</h2>

@if (!string.IsNullOrEmpty(ViewBag.Message))
{
    <div class="alert alert-success" role="alert">@ViewBag.Message</div>
}

<div class="row">
    <div class="col-xs-12">
        <a href="@Url.Action("CreateUser")" class="btn btn-success">
            <span class="glyphicon glyphicon-plus-sign"></span>
            Create New User
        </a>
    </div>
</div>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Username</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model.UserDictionary)
        {
            <tr>
                <td>
                    @user.Value
                </td>
                <td>
                    @Html.ActionLink("Reset Password", "ResetUserPassword", new { id = user.Key }, new { @class = "btn btn-default btn-sm" })
                    @Html.ActionLink("Delete Account", "DeleteUser", new { id = user.Key }, new { @class = "btn btn-danger btn-sm" })
                </td>
            </tr>
        }
    </tbody>
</table>